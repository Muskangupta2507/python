'''Write a function Bubble_Sort() to sort a list inputted by user.'''
 
def Bubble_Sort(arr):
    n = len(arr)
    for i in range(n-1):
        for j in range(0, n-i-1):
            if arr[j] > arr[j + 1] :
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
arr =list(map(int,input().split()))
Bubble_Sort(arr)
l=list()
print ("Sorted list is:")
for i in range(len(arr)):
    l.append(arr[i])
print(l)
