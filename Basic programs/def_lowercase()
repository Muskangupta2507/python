""" WAP to define a function to lowercase(s) which takes a string s
to convert string into lowercase string without using predefined method"""
    for i in range(d):
        if s[i]>=chr(65) and s[i]<=chr(90):
            y +=chr(ord(s[i])+32)
        else:
            y +=chr(ord(s[i]))
    return(y)
a=input()
c=uppercase(a)
print(c)
